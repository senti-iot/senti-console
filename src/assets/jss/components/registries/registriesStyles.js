import { makeStyles } from '@material-ui/styles';

const registriesStyles = makeStyles(theme => ({

	root: {
		width: '100%',
		margin: theme.spacing(1),
		borderRadius: "3px",
	}
}))

export default registriesStyles;
